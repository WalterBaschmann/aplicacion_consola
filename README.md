-------Ejercicio 1--------

-Aparece un alert → “entraste en el sitio”/

-Luego aparece un prompt → “dime tu nombre”/

-El usuario escribe su nombre/

-El nombre se imprime en la consola/

-------Ejercicio 2--------

Se preparan las variables

Se declaran numero1, numero2 y el mensaje "Operaciones matemáticas".

El programa pide el primer número

Aparece un prompt solicitando el primer número.

El usuario ingresa un valor.

El programa pide el segundo número

Aparece otro prompt.

El usuario ingresa el segundo valor.

Se realizan todas las operaciones

Se calcula:

  suma

  resta

  multiplicación

  división

(aunque el usuario aún no elige cuál quiere ver).


Se muestran los resultados en consola


Se imprime el mensaje "Operaciones matemáticas".

Se muestran en consola los resultados de todas las operaciones.

Se valida la división por cero

Si el segundo número es 0, se muestra un mensaje de error.

Si no es 0, se indica que la división es válida.


El usuario elige una operación

Aparece un prompt para elegir:

  suma

  resta

  multiplicacion

  division


Se evalúa la opción elegida

El switch revisa qué operación escribió el usuario.

Según la opción:

  Se muestra solo el resultado de esa operación.

  Si eligió división y el segundo número es 0, se muestra un error.

  Si la opción no coincide con ninguna, se muestra “Operación no válida”.

-------Ejercicio 3--------

Se crea un arreglo vacío

numeros comienza sin elementos.

Se llena el arreglo

Un for va desde 1 hasta 20.

En cada vuelta se agrega el número al arreglo.

Al final, el arreglo contiene los números del 1 al 20.

Se recorre el arreglo con for

Se inicia desde la posición 0 hasta la última.

En cada iteración se muestra un número en consola con el texto "For:".

Se recorre el mismo arreglo con while

Se inicia un contador en 0.

Mientras el contador sea menor al tamaño del arreglo:

Se muestra el número en consola con "while:".

Se incrementa el contador.

Se define la función filtrarPares

Recibe un arreglo como parámetro.

Crea un nuevo arreglo vacío para guardar números pares.

Recorre el arreglo recibido.

Si un número es par, lo agrega al nuevo arreglo.

Devuelve el arreglo con solo los pares.

Se filtran los números pares

Se llama a la función usando el arreglo numeros.

El resultado se guarda en numerosPares.

Se muestran los números pares

Se imprime en consola el arreglo con los números pares.

-------Ejercicio 4--------

Se definen las funciones matemáticas

Se crean funciones para:

sumar

restar

multiplicar

dividir

La división valida si el segundo número es cero.

Se define la función calcular

Esta función recibe:

el nombre de la operación

dos números

Según la operación indicada:

llama a la función correspondiente

devuelve el resultado

Si la operación no existe, devuelve un mensaje de error.

El programa pide el primer número

Aparece un prompt solicitando un número.

El usuario ingresa el valor.

El programa pide el segundo número

Aparece otro prompt.

El usuario ingresa el segundo valor.

Se ejecutan los cálculos

Se llama a calcular cuatro veces:

una para suma

una para resta

una para multiplicación

una para división

Se muestran los resultados

Cada resultado se imprime en la consola con su nombre.

-------Ejercicio 5--------

Se crea un objeto producto

Representa un producto con nombre, precio y stock.

Incluye métodos para:

  mostrar la información del producto
  
  verificar si hay stock

Se crea un arreglo productos

Contiene varios productos, cada uno con nombre y precio.

Se recorre el arreglo con forEach

El programa pasa por cada producto del arreglo.

En cada recorrido:

  Muestra en consola el nombre y el precio del producto.

Se crea un nuevo arreglo con map

Se recorre nuevamente el arreglo productos.

De cada producto se extrae solo el nombre.

Se genera un nuevo arreglo con los nombres.

Se muestran los nombres

Se imprime en consola el arreglo con los nombres de los productos.
